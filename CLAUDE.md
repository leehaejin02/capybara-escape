# Capybara Escape — 프로젝트 규칙

NAN 2026 사전 과제. **접수 마감 2026-08-10** (기획 시작 8/6, 가용 4일).

> **제출물 중 하나라도 누락되면 심사 대상에서 제외된다.**
> 게임 완성도보다 제출물 완비가 절대 우선이다. 충돌하면 게임 기능을 자른다.
>
> **3인 팀 참여 확정 (2026-08-09 변경)** → 우리가 낼 것은 **5종**: 빌드+소스 / 플레이 영상 /
> 게임 소개 PDF / **AI 활용 기술 PDF** / **팀원 롤 기술서 PDF**.
> 팀: 이해진(팀장·개발) · 지승제(기획·문서) · 유한결(아트·영상). 본선 3인 전원 참석 가능.
> **사전 과제 구현은 팀장 단독 수행이 사실이며, 롤 기술서에 그대로 적는다.** 허위 기재는 선발 취소 사유다.

## 세션 시작 시 반드시

1. `WORKLOG.md` — 지금까지 뭘 했고 다음에 뭘 할지
2. `docs/SUBMISSION.md` — 제출물 5종 체크리스트. **남은 시간을 여기에 먼저 배분한다**
3. `docs/GDD.md` — 게임 기획서. 특히 **10장 스코프 라인**과 **11장 완료 기준**
4. `docs/DECISIONS.md`(있으면) — 이미 내린 결정. 다시 논의하지 않는다

## 에이전트 팀

| 에이전트 | 담당 | 소유 파일 |
|---|---|---|
| `director` | 인텐트 → 메타 프롬프트 → 라우팅 | (없음) |
| `gd` | 무엇을 만들까 / 규칙 · 수치 | `docs/`, `src/config/balance.ts` |
| `tech` | 어떻게 만들까 / 코드 | `src/` (balance.ts 제외) |
| `playtest` | 수치가 맞는가 (헤드리스 시뮬) | (읽기 전용) |
| `verify` | 기준을 만족하는가 | (읽기 전용) |

기본 흐름: **director → gd → 사용자 승인 → tech → playtest → verify**
단계를 건너뛰는 건 허용된다. 단 **왜 건너뛰는지 반드시 밝힌다.** 조용한 생략은 금지다.

## 하네스 — 절대 하면 안 되는 것

### 아키텍처
1. **`src/sim/` 아래에서 Phaser를 import하지 않는다.** 게임 로직은 브라우저 없이 Node에서 돌아야 한다. 이 경계가 무너지면 `playtest` 에이전트가 성립하지 않고, 그건 이 프로젝트의 핵심 차별점을 잃는 것이다.
2. **밸런스 수치는 `src/config/balance.ts`에만 존재한다.** 속도·시야·제한시간·쿨다운을 코드에 직접 쓰지 않는다.
3. 렌더링 씬에서 게임 규칙을 판정하지 않는다. 씬은 sim의 상태를 그리기만 한다.

### 보안
4. **비밀값은 어떤 형태로도 저장소에 들어가지 않는다.** 키·토큰·비밀번호를 코드·문서·설정에 쓰지 않는다.
5. **비밀값이 포함된 명령을 실행하지 않는다.** 명령 전체가 허용 목록(`.claude/settings.local.json`)에 문자열로 기록돼 커밋될 수 있다.

### 스코프 (마감이 4일이라 이게 하네스다)
6. **`docs/GDD.md` 10장의 WON'T 항목을 만들지 않는다.** 멀티플레이·서버·맵 2개 이상·스토리·모바일 대응·세이브.
7. 스코프를 늘리려면 **동시에 무엇을 버릴지 같이 제시**한다. 순증가 제안은 거절한다.
8. 새 npm 의존성은 **승인 없이 추가하지 않는다.** Phaser·Vite·TypeScript 외에는 기본 거절.

### 대회 제출 규정 (위반 = 심사 제외 또는 선발 취소)
9. **커밋을 마지막에 몰아서 하지 않는다.** 작업 단위마다 커밋한다. "커밋 기록 유지"가 제출 요건이고, 에이전트가 단계별로 일한 흔적이 곧 「AI 활용 기술 문서」의 물증이다.
10. **외부 에셋·오픈소스를 가져오는 순간 `docs/ASSET_CREDITS.md`에 출처와 라이선스를 적는다.** 나중에 추적하지 않는다. 무단 도용은 선발 취소 사유다.
11. **유료 라이선스가 필요한 것을 쓰지 않는다.** 심사자가 추가 비용 없이 실행할 수 있어야 한다.
12. **플레이 영상은 실제 플레이 화면만 쓴다.** AI 합성·조작·타인 영상 도용은 규정 위반이다.
13. 제출 후에는 내용을 변경할 수 없다. **마감 당일에 처음 배포하지 않는다** — 배포는 하루 앞서 끝낸다.

### 정직성
14. **확인 못 한 것을 통과로 보고하지 않는다.** 실행 대신 정적 분석·계산으로 대체했으면 "실측 아님"을 반드시 명시한다.
15. **작업 중 이상한 걸 발견하면 무조건 말한다.** 스펙 모순, 기존 코드가 문서와 다르게 동작, "완료"로 적혀 있는데 실제로는 안 되는 것 — 조용히 우회하지 않는다.
16. 밸런스는 감으로 확정하지 않는다. **`playtest` 측정 없이 "밸런스 맞췄다"고 쓰지 않는다.**

## 발화 → 절차 연결

사람이 기억하지 않아도 절차가 돌아가게 한다.

| 사용자가 이렇게 말하면 | 이렇게 한다 |
|---|---|
| "○○ 만들어줘" / "○○ 바꾸자" | `director`부터 호출 (작은 일이면 director가 스스로 불필요 판정) |
| "다 됐나?" / "완료됐어?" | `verify` 호출. 그 전엔 완료라고 말하지 않는다 |
| "밸런스 어때?" / "너무 쉬워/어려워" | `playtest` 호출 후 그 숫자로 답한다 |
| "커밋해줘" / "정리하자" | WORKLOG 갱신 → 타입체크 → 커밋. `.claude/` 산출물은 **공유 자산이라 무조건 포함** |

## 기록

- **WORKLOG.md**: 세션마다 한 일 / 결정한 것 / 다음 할 일 (날짜 역순)
- **docs/DECISIONS.md**: 되돌리기 어려운 선택과 **그 이유**. 이유 없는 결정은 다음 세션에서 다시 논의된다
- 에이전트·커맨드 호출 횟수는 제출 근거가 된다. 마감 전 한 번 집계한다 (0건인 에이전트가 있으면 그 자체가 결함)

## 작업 방식

- 되돌리기 어려운 것(배포, 의존성 추가, 파일 대량 삭제, 스코프 변경)은 **승인받고** 진행한다. 설명만 하고 진행하는 것은 승인이 아니다.
- 작업 성격이 바뀌면(기획 ↔ 구현) 컨텍스트 사용률과 무관하게 **세션을 새로 연다.**
- 한 번에 한 축만 바꾼다. 여러 수치를 동시에 바꾸면 무엇이 효과였는지 알 수 없다.
