# WORKLOG

> 최신순. 세션마다 **한 일 / 결정한 것 / 다음 할 일**을 남긴다.

## 2026-08-06~07 (세션 3) — sim 구현 + 배포 + playtest 첫 측정

### 완료된 MUST (GDD 10장)

- [x] **GitHub Pages 배포 + 공개 저장소** — https://leehaejin02.github.io/capybara-escape/
- [x] **헤드리스 시뮬 + playtest 밸런싱** — 1회차 측정 완료 (합격은 아님)

### 한 일

- `docs/RULES.md` 신설 (gd) — 50×37 맵, FSM 판정식, 봇 정책. 헤드리스로 계산 가능한 수준
- `src/sim/` 규칙 구현 (tech) — map/raycast/vision/movement/goblin/mission/bot/world. **`stub` 해제**
- `docs/ART.md` 신설 (gd) — 16색 팔레트 + 스프라이트 명세
- `docs/ASSET_CREDITS.md` 신설 — sprite-gen(Apache-2.0) 도입 기록
- sprite-gen 환경 구축 — Python 3.12.10 설치, venv + Pillow/numpy
- `playtest` 첫 호출 — 1200회 측정

### 배운 것 1 — 문서의 안전 논증에 구멍이 있었다

`RULES.md` §3.5가 "터널링 없음"을 논증하는데, 그 논증은 **틱당 이동(≤2.5px)만** 커버한다.
`HIT.KNOCKBACK_PX`(64px)는 그 밖이었고, 한 번에 이동시키면 **플레이어가 벽을 뚫고 맵 밖으로 나갔다.**
tech가 구현 중 실측으로 발견 → 서브스텝 분할로 수정. 재현 시드·틱을 코드 주석에 기록.

> 문서가 "안전하다"고 논증했다고 해서 안전한 게 아니다. 논증의 **적용 범위**를 확인해야 한다.

### 배운 것 2 — 지표가 다른 지표의 증상일 수 있다

`stuckRuns`(봇 정지 감지)가 2/100으로 0이 아니었다. 봇 버그인지 밸런스 문제인지가 쟁점이었다.

`playtest`가 **반증 실험**으로 판정했다: `MAX_HP`를 99로 올려 사망을 제거하자
`stuckRuns`가 줄기는커녕 **2·1·2 → 7·14·12로 급증**했다.
→ 원래 낮았던 건 봇이 잘 움직여서가 아니라 **60초 체크포인트 전에 이미 죽어서 검사를 못 받았기 때문**.
`stuckRuns`는 처음부터 치사율의 파생 지표였다.

> "지표가 좋아 보인다"가 "문제가 없다"를 뜻하지 않는다. 그 지표가 측정될 기회 자체를 잃었을 수 있다.

### playtest 1차 결과 — 전면 미달

6시드 × 200회 = **1200회** 합산 (시드 간 clearRate 표준편차 1.7%p)

| 지표 | 목표 | 실측 |
|---|---|---|
| 클리어율 | 35~55% | **7.0%** |
| 평균 잔여시간(승리 시) | 10~35s | **39.4s** |
| 평균 피격 | 1~2회 | **2.84** (MAX_HP 3) |
| timeout:hp0 | 6:4 | **1.4% : 98.6%** |

**단일 원인: 치사율 과다.** 시간은 오히려 남는다. 병목은 미션 소요가 아니라 고블린 접촉.

### 발견한 모순 → **해소됨** (D7-a)

**`GOBLIN.CHASE_SPEED`(95) > `PLAYER.BASE_SPEED`(90)** 인데,
GDD 6장 본문과 `balance.ts` 주석은 둘 다 **"플레이어가 더 빠르다"**고 적혀 있었다. 셋이 서로 모순.

`gd` 판정: **수치가 틀렸다.** 두 문장이 *서로 독립적으로* 같은 의도를 말하고 있었으므로,
오타 둘이 같은 방향으로 날 확률보다 숫자 하나가 잘못 굳었을 확률이 높다.
95에서는 대시를 끊김 없이 써도 3초당 9px밖에 못 번다 → 실질 도주 수단이 "시야 이탈 3초" 하나뿐이었다.

→ **95 → 75.** 부등호 `CHASE < BASE_SPEED`를 **불변식으로 승격**(GDD 6장 + balance.ts).
앞으로 이 부등호는 밸런싱 대상이 아니고, 상대 속도 조정 시 움직이는 쪽은 **항상 고블린**이다.

### 배포 — 5회 실패 후 방식 전환

`actions/deploy-pages` 경로가 이 저장소에서 프로비저닝되지 않았다(5회 모두 `deployment_queued` 정체).
빌드·워크플로·브랜치 정책·Pages 서비스 전부 정상임을 확인한 뒤 **`gh-pages` 브랜치 배포로 전환** → 한 번에 성공.
Pages 배포 ID는 커밋 SHA이고, 서버에 걸린 배포가 후속 배포를 전부 막는다는 것도 확인했다.

> 하네스 13(마감 당일 첫 배포 금지)이 값어치를 한 지점. 8/10이었으면 여기서 끝났다.

### ⚠️ 배운 것 3 — 측정 기구가 고장 났을 수 있다 (라운드 2, 미해결)

라운드 2(`CHASE_SPEED` 95→75) 결과: 클리어율 7.0% → **12.9%**. 방향은 맞지만 여전히 미달.
`gd`의 D7-d 분기표상 "20% 미만" 갈래이고, 그 갈래가 **봇 실효 이동속도 측정**을 지시했다.
그 진단이 숫자보다 중요했다.

| 측정 | 값 |
|---|---|
| 이론상 이격 속도 (`BASE 90 − CHASE 75`) | 15 px/s |
| **실측 이격 속도** (CHASE 중 고블린 300px 이내 전 틱) | **2.2 px/s** — 이론값의 15% |
| 순간 속도 실현율 (벽 충돌 손실) | 90.8% — 9% 손실로는 설명 안 됨 |
| 자유이동 경로 우회 배수 | 평균 2.38× (p90 9.9×, p99 37×) |
| 20초 넘는 이동 구간 | 전체 이동거리의 **56.6%** |

**유력 원인**(playtest 분석, tech 확인 필요): `src/sim/bot.ts`의 `computeFleeDir`가
위협들로부터의 **단위벡터 합**을 쓴다. 고블린 2마리 이상이 서로 다른 방향에서 잡히면
**합이 상쇄돼** 도주 방향이 흔들리거나 0에 가까워진다. 여기에 384px 간격의 성긴
12노드 격자가 LOS 차단 시마다 개입한다.

**왜 심각한가**: `RULES.md` §7.0 — *"게임을 못 고쳐서 봇을 고치면 측정이 무의미"*.
반대도 성립한다. **고장 난 자로 재면서 게임 수치를 깎으면 사람에게는 터무니없이 쉬운 게임이 된다.**
`gd`도 D7-e에 "봇은 격자를 우회하지만 사람은 직선으로 움직인다"고 이미 적어 뒀다.
봇이 못 도망치는 걸 보상하려고 시야를 90까지 내리면 그 오차가 그대로 게임에 박힌다.

→ **VISION 조정 후에도 목표에 못 들어가면, 다음 후보는 `balance.ts` 축이 아니라
`bot.ts`의 FLEE 벡터합 / 격자 밀도다.**

### 검증된 예측 2건 (라운드 2)

- **D7-c (gd)** — avgHits는 독립 축이 아니라 hp0 비율의 파생 지표다.
  산술 모델(`0.1292×1 + 0.0867×1.5 + 0.7842×3 = 2.61`)이 **실측 2.61과 소수점 둘째 자리까지 일치.** 확정.
- **stuckRuns (playtest)** — 치사율이 내려가면 같이 준다. hp0 91.7%→78.4% 동안 1.25%→0.83%. 맞았다.

`playtest`는 자기가 인용한 효율 추정도 정정했다: CHASE 축이 **수확 체감** 구간에 들어섰다
(95→85 구간 0.408 %p/1% → 85→75 구간 **0.136**). gd가 말한 "VISION 대비 2배 효율" 격차는 사라졌다.

**라운드 4 입력값**(측정해 둠): 승자 잔여시간 중앙값 **48.1s**, elapsed 중앙값 131.9s.
라운드 3에서 분포가 바뀌면 재계산 필요 — 지금은 참고치.

### 다음 할 일

1. **gd 조정 반영 → playtest 재측정** (수렴까지 여러 라운드 예상)
2. **스프라이트 생성 스크립트** — `docs/ART.md` 명세대로. 가장 어려운 건 `capy_body_01` right 3/4
   (여기서 리컬러 64프레임 + flipX 8프레임 + 오버레이 9종 앵커가 파생된다)
3. **렌더링 씬** — S1~S4. sim 상태를 그리기만 한다
4. 커스터마이즈 3레이어 합성 + 인게임/결과 반영
5. 미션 3종 UI
6. 마감 전날: 60초 플레이 영상 → YouTube, PDF 2종

## 2026-08-06 (세션 2) — 스캐폴딩 + 헤드리스 시뮬 러너

**파이프라인 전 구간을 처음으로 한 바퀴 돌린 세션.** `director → gd → 승인 → tech → verify`.

### 한 일

- **GitHub 공개 저장소 생성 + push** — https://github.com/leehaejin02/capybara-escape (브랜치 `master` → `main`)
- `director` 호출 → 메타 프롬프트 + 라우팅 + 승인 항목 6건 도출
- `docs/DECISIONS.md` 신설 (D1~D6)
- `gd` → `src/config/balance.ts` (GDD 5·6·7장 수치 이식, import 0개 / 파생식 0개)
- `tech` → Vite + Phaser 3 + TS 스캐폴딩 + `npm run sim` + `scripts/check-boundary.mjs`
- `verify` → 완료 기준 11항목 + 하네스 6항목 **직접 재실행** 감사

### 이 세션의 핵심 — 경계 장치가 "3중"이 아니었다

착수 시엔 `check-boundary.mjs` + `tsconfig.sim.json` + 런타임 크래시의 **3중 방어**를 기대했다.
`tech`가 구현 중 **실측으로 2차가 작동하지 않음을 발견**했고(sim에 phaser import를 넣어도 `tsc`가 exit 0),
사용자가 이를 독립 재현했으며(`paths` 리맵 우회도 실패), `verify`가 문서에 남은 과장 표현 2곳을 잡아냈다.

→ **실질 게이트는 `scripts/check-boundary.mjs` 하나뿐이다.** 상세는 `docs/DECISIONS.md` "D1 정정".
→ 그래서 검사기를 `npm run sim`뿐 아니라 **`npm run build`에도** 걸었고, 스캔 0개면 스스로 exit 1 하게 했다.
→ `verify`가 import 형태 **13종**으로 교차 검증 (타입 전용·동적·`require`·다중행·주석/문자열 안 가짜 import).
   **오탐·미탐 0건.**

> 에이전트가 자기 설계의 오류를 실측으로 잡고, 다른 에이전트가 문서의 과장을 잡았다.
> 「AI 활용 기술 문서」에 그대로 들어갈 사례다.

### 결정한 것

`docs/DECISIONS.md` D1~D6 참조. 요약: `@types/node`만 승인 / tsc 선컴파일 / CLI 종료코드 0·1·2 /
스텁 시뮬 수용 / 씬 껍데기 안 만듦 / `balance.ts`는 `gd` 소유.

### 확인 못 한 것 (하네스 14)

- ~~`npm run dev` 화면 육안 확인 미실시~~ → **해소.** 브라우저에서 직접 확인: 타이틀 "Capybara Escape"가
  800×600 캔버스에 렌더되고, Phaser v3.90.0이 WebGL + Web Audio로 부팅. **콘솔 에러 0건.**
- **밸런스 수치는 전부 미검증 가설.** 게임 규칙이 없어 `playtest` 측정 자체가 불가능 (D5, 명시적 생략)
- `check-boundary.mjs` 알려진 한계 2건: 템플릿 리터럴 동적 import, 변수로 만든 지정자 (정적 스캐너의 구조적 한계)

### Pages 배포 — 1·2차 실패와 원인 (하네스 13이 값어치를 한 지점)

`gh api ... -f build_type=workflow`로 Pages를 켠 직후 배포를 돌렸다.

| 시도 | 결과 | 원인 |
|---|---|---|
| 1차 (push) | 빌드 ✅ / 배포 ❌ | `deployment_queued`로 10분 정체 후 타임아웃. Pages 사이트 프로비저닝이 끝나기 전에 첫 배포가 들어감 |
| 2차 (`workflow_dispatch`, 같은 커밋) | 즉시 ❌ | **Pages 배포 ID = 커밋 SHA.** 1차에서 그 SHA가 "취소됨"으로 박혀, 같은 커밋 재실행은 즉시 cancelled |

**배운 것: 같은 커밋으로 재실행하면 영원히 안 된다. 새 커밋 SHA가 필요하다.**
확인한 것 — `github-pages` 환경의 브랜치 정책은 `main` 허용으로 정상, 기본 브랜치도 `main`.
워크플로·`base` 경로·빌드 산출물에는 문제가 없었다 (빌드 job은 두 번 다 13초에 통과).

> 이걸 8/10에 처음 돌렸으면 빌드가 멀쩡한데 사이트가 안 뜨는 상태로 마감을 맞았을 것이다.
> 하네스 13("마감 당일에 처음 배포하지 않는다")이 정확히 이 상황을 위해 있다.

### 남겨둔 사소한 것 (지금 고치지 않기로 함)

- **Vite 경고**: `vite.config.ts`가 CJS로 로드된다는 경고. 해결책인 `package.json`에 `"type": "module"`
  추가는 **`npm run sim`을 깨뜨린다** — `tsconfig.sim.json`이 CommonJS로 출력하는데
  `dist-sim/*.js`가 ESM으로 해석되기 때문이다. 고치려면 sim 출력 모듈 형식까지 같이 바꿔야 하므로
  경고를 남겨둔다. 빌드·실행에는 영향 없음
- 캔버스가 페이지 좌상단에 붙어 있다 (센터링 CSS 없음). 렌더링 씬 작업 때 같이 처리한다

### 발견한 GDD 불일치 (미해결)

맵 세로 **1200px ÷ 타일 32px = 37.5** — 정수 타일이 아니다 (가로는 1600÷32=50으로 정상).
`gd`가 GDD 7장(1200 → 1184 또는 1216)이나 8장 중 하나를 고쳐야 한다. **타일맵 생성 전까지는 미룰 수 있다.**

### 다음 할 일

1. **`gd` → 맵 세로 크기 불일치 해소** (타일맵 착수 전 필수)
2. **`gd` → sim 게임 규칙 스펙 확정** — 이동·충돌·고블린 FSM·시야·미션 진행을 헤드리스로 판정 가능한 형태로
3. **`tech` → `src/sim/`에 규칙 구현 + 봇 정책** → 이때 `stub: true`를 내린다
4. **`playtest` 최초 호출** — 3번 직후. 이때까지 "밸런스 맞췄다"는 말을 쓰지 않는다 (하네스 16)
5. `GameScene` 등 렌더링 씬 — sim 상태를 그리기만 한다
6. **GitHub Pages 배포 파이프라인 — 8/9까지.** 마감 당일 첫 배포 금지 (하네스 13)
7. 마감 전날: 플레이 영상 촬영 → YouTube, PDF 2종

## 2026-08-06 (세션 1) — 프로젝트 개설

### 한 일
- NAN 2026 대회 조건 조사 (접수 마감 8/10, 본선 9/4~6 48시간, 주제 현장 공개, 제출물에 **AI 에이전트 설계서·디렉팅 명세서** 포함)
- 레퍼런스 확인: *In the Van* = 2D 탑다운 픽셀아트 생존 시뮬
- `docs/GDD.md` 초안 작성 (v0.1)
- 에이전트 5종 등록: `director` / `gd` / `tech` / `playtest` / `verify`
- `CLAUDE.md` 하네스 작성 (아키텍처 3 / 보안 2 / 스코프 3 / 정직성 3)

### 결정한 것
| 결정 | 이유 |
|---|---|
| 웹 + Phaser 3 + TypeScript + Vite | 심사위원이 링크만 열면 즉시 플레이. 타일맵·충돌·애니메이션 내장이라 4일 압축에 유리 |
| 싱글 플레이 + AI 고블린 | 서버·동기화·매칭이 빠지면서 4일 안에 완주 가능해짐. 어몽어스에서 "시간 내 미션 후 탈출" 구조만 추출 |
| 에이전트 5개로 시작 (7개 아님) | "처음부터 완벽한 체계 금지" 원칙. `artdir`·`chronicler`는 실제로 막힐 때 추가 |
| `src/sim/`을 Phaser 비의존으로 격리 | 헤드리스 시뮬이 가능해야 `playtest` 에이전트가 성립. **이 프로젝트 최대의 차별점이 이 경계 위에 있다** |
| 밸런스 수치를 `balance.ts` 한 파일로 격리 | gd와 playtest가 파일 하나만 보고 왕복할 수 있게 |
| 커스터마이즈는 성능에 영향 없음 | 4일 안에 밸런싱할 축을 늘리지 않기 위해 |

### 블로커 해소 — 사전 과제 명세 확보 (같은 세션, 크롬 연결 후)

공식 페이지의 「상세보기」 모달을 직접 열어 원문 확인 → `docs/SUBMISSION.md`에 정리.

**계획을 바꾼 사실 5가지**

| 발견 | 영향 |
|---|---|
| 제출물 **5종 중 하나라도 누락 시 심사 제외** | 게임 완성도보다 제출물 완비가 절대 우선. 스코프 판단 기준이 바뀜 |
| 웹 빌드 + **GitHub Pages**, **.exe 제출 불가** | Phaser 웹 선택이 정확히 맞았음 (운이 아니라 확인됨) |
| 전체 소스 동일 저장소 + **커밋 기록 유지** | 몰아 커밋 금지. 에이전트 단계별 커밋이 곧 「AI 활용 기술 문서」의 물증 |
| **30~60초 실제 플레이 영상** 필수, AI 합성·조작 금지 | 게임이 1분간 안 죽고 돌아야 함. 촬영·업로드 시간 확보 필요 |
| **「AI 활용 기술 문서」**에 *구조 설명 + AI 대상 주요 프롬프트 및 지시 사항* 요구 | 우리 접근이 정면으로 맞아떨어짐. CLAUDE.md·agents·playtest 로그가 그대로 본문 |

부수 확인: 팀당 **최대 3인** 확정(기사의 "2~5명"은 오보). 개인 참여면 5번 제출물 생략.
외부 에셋은 출처·라이선스 명시 필수, 무단 도용 시 **선발 취소**. 접수 마감 후 변경 불가.

→ 이에 따라 `CLAUDE.md`에 **대회 제출 규정 하네스 5조(9~13번)** 추가, GDD 0·8·10장 개정.

### 결정: 개인 참여 (2026-08-06)

제출물이 **4종**으로 확정됐다 (「팀원 롤 기술서」는 2인 이상 팀만 해당).

- PDF가 하나 줄어 마감 압박이 완화됨
- 대신 **「AI 활용 기술 문서」가 팀원 롤 기술서의 역할까지 흡수**한다.
  규정이 팀에게 요구하던 *"팀원별 담당 역할 / 실제로 맡아 구현한 영역 / 협업·분업 방식"* 을
  **에이전트 5종에 대입해** 쓴다 — 사람 대신 에이전트에게 역할을 나눈 구조라는 게 곧 주장이다
- "혼자서 4일에 이만큼"이라는 사실 자체가 파이프라인의 설득력이 된다

### 다음 할 일
1. **GitHub 공개 저장소 생성 + 첫 push** — 커밋 기록이 제출 요건이라 하루라도 일찍 시작할수록 유리
3. Vite + Phaser 3 + TS 스캐폴딩, `src/sim/` ↔ `src/scenes/` 경계 먼저 세우기
4. 헤드리스 시뮬 러너 `npm run sim` — **게임보다 먼저 만든다.** 나중에 붙이면 아키텍처가 이미 망가져 있다
5. `balance.ts` 초기값 이식 (GDD 7장)
6. GitHub Pages 배포 파이프라인 — **8/9까지 끝낸다.** 마감 당일 첫 배포는 금지(하네스 13)
7. 마감 전날: 플레이 영상 촬영 → YouTube, PDF 3종 생성

### 확인 못 한 것
- 신청서(Google Forms)의 실제 입력 항목 — 폼 제출은 본인이 해야 함
- FAQ Q04(시연·발표 형식), Q05(유료 API 부담) — 본선 사항이라 사전 과제엔 영향 없음
