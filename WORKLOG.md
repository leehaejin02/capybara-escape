# WORKLOG

> 최신순. 세션마다 **한 일 / 결정한 것 / 다음 할 일**을 남긴다.

## 2026-08-07 (세션 5) — 맵 아트 개편 + **발주 오독 정정(맵 3종)** + UI 전면 개편

> **다음 세션은 이 항목의 「바로 시작하기」부터 읽으면 된다.**

### 🚀 바로 시작하기 (다음 세션 첫 행동)

**게임 기능을 더 만들지 마라. 남은 MUST 3종을 잡는다.**

| # | 제출물 | 상태 |
|---|---|---|
| 1 | 빌드 + 소스 (GitHub Pages) | ✅ 있음. **단 이번 세션 변경분이 아직 배포 안 됨** |
| 2 | **30~60초 플레이 영상** | ❌ **미착수** |
| 3 | **게임 소개 PDF** | ❌ **미착수** |
| 4 | **AI 활용 기술 PDF** | ❌ **미착수** |

**하나라도 누락되면 게임 완성도와 무관하게 심사 제외다.** 남은 날은 8/8·8/9·8/10뿐이고,
하네스 13상 **배포는 8/9까지 끝나야 한다**(마감 당일 첫 배포 금지).

권고 일정 — **사용자에게 아직 승인받지 못했다. 먼저 확인할 것:**

| 날짜 | 할 일 |
|---|---|
| **8/8** | 세 맵 직접 플레이(아래 미해결 판정) → 영상 촬영 |
| **8/9** | **PDF 2종 + 최종 배포.** 이날 안에 배포 끝 |
| 8/10 | 예비 + 접수 |

**영상 촬영이 곧 밸런스 실측이다.** 피격·완료 미션·잔여 시간·**어느 맵이 나왔는지**를 기록해라.
D8-e가 "봇 기준 추정이 사람에게 얼마나 빗나가는지"의 반증 자료로 지목한 값이고 추가 비용 0이다.

### ⚠️ 미해결 판정 1건 — 다음 세션이 결정해야 한다

**맵별 클리어율이 문턱을 크게 위반했다** (각 1200회, `npm run sim -- --map=N`):

| 맵 | clearRate | avgHits | avgMissions | stuckRuns |
|---|---|---|---|---|
| 0 village | **13.6%** | 2.58 | 2.83 | 0.8% |
| 1 forest | **4.0%** | 2.80 | 1.33 | **27.6%** |
| 2 cave | **1.7%** | 2.55 | 2.36 | 0.0% |

- **PG2 위반**: 13.6/1.7 = **8.13** (문턱 `TARGET.MAP_CLEAR_RATE_RATIO_MAX` 2.5)
- **PG3**(보고만): avgMissions 폭 1.50 (문턱 1.0)
- **봇 무관 기하 게이트 PG1/PG1b는 세 맵 전부 통과**: P→E 68/68/68(비율 1.00), P→M 중앙값 37/32/36(비율 1.156)

**`tech`가 스펙 모순을 잡고 임의 판단을 거부했다.** `SPEC_MAPS.md` §6.3은 *"중앙값에서 가장 먼 맵을
뺀다"*고 하는데, 실측 {13.6, 4.0, 1.7}에서 median=4.0이고 거리는 village(9.6) > cave(2.3)이라
**가장 잘 나온 village가 제거 대상**이 된다. §1.1의 *"village는 항상 되돌림 기준선"*과 정면 충돌한다.
→ **`MAPS`는 3개 그대로 뒀다.** `gd`/사용자 판단 대기.

**코디네이터 권고: 3개 그대로 간다.** 봇이 도주를 실행하지 못하므로(D8) 이 값들은 사람 클리어율의
**하한**이고, 봇과 무관한 기하 게이트는 전부 통과했다. D8이 *"고장 난 자로 재면서 게임 수치를 깎지
않는다"*고 정했는데 **지금 맵을 빼는 것도 같은 자로 재서 자르는 것**이다.
→ 대신 **8/8에 세 맵을 직접 플레이해 클리어 가능한지 확인한다.** 영상 촬영과 겸하므로 비용 0.

⚠️ **D8 이전 수치(7.0% / 12.9% / 15.0%)와 직접 비교 불가.** 맵이 3개가 되며 판 내용이 전부 바뀌었다.
`playtest`는 기준선을 다시 잡아야 한다.

### 한 일

**커밋 3건**: `e79dc1d` 벽 3단+구역 / `df1869d` 소품+수풀+캐노피+숲 개정 / `71ad8cf` 맵 3종+UI

1. **SPEC_ZONES 전 구현** — 팔레트 16→30색, 벽 3단 구조(`tile_wall` 128×96, 남북 2비트 이웃 마스크),
   드롭섀도, 구역별 바닥, 소품 12종(결정적 배치), 수풀·온천 구역화, 은신 캐노피
2. **맵 3종 랜덤 선택** — 발주 오독 정정(아래)
3. **UI 전면 개편** — 사용자 판정 "너무 형편없다"

### 🔴 이 세션의 핵심 — 발주를 재해석하고 묻지 않았다 (D12)

사용자의 원래 지시는 **"맵이 3개인데 랜덤하게 나온다"**였다. `gd`는 이를 **한 맵의 세로 3구역**으로
해석했고 **코디네이터가 그대로 승인했다.** 사용자가 직접 플레이해 보고서야 잡혔다.

**신호는 있었다.** `SPEC_ZONES.md` §5.1에 *"발주 지시와 남북을 뒤집었다"*고 **대놓고 적혀 있었다.**
지시를 재해석하고 있다는 자백인데 아무도 멈추지 않았다.

> **D12 규칙**: `gd`가 발주 문장을 다른 뜻으로 바꿔 읽으면, 스펙을 쓰기 전에 **한 줄 질문으로
> 되돌린다.** 문서에 *"지시와 다르게 했다"*고 **쓰고 싶어지는 순간이 바로 그 지점**이다.

**D10 계열이지만 더 나쁘다.** 앞선 3건은 "안 적었다"였는데 이번은 **"적었는데 안 물었다"**다.
코드는 스펙대로 완벽히 동작했으므로 `tech`도 `verify`도 **실행으로 잡을 수 없는 유일한 종류의
결함**이다 — 사용자가 플레이해야만 잡힌다. 「AI 활용 기술 문서」에 넣을 사례.

**손실은 작았다.** 타일 3세트가 이미 구워져 있어 **아트 재작업 0건**이었고,
`zoneOfRow(행)` → `activeMap.zone`으로 **렌더가 오히려 단순해졌다.**

### 배운 것 6 — 한 문제를 고쳐서 다른 문제를 만들 수 있다

1단계에서 **코디네이터가** "숲 벽 윗면이 체커보드 격자로 보인다"고 반려했다. 원인은 스펙 §3.3의
**"체비쇼프"** 문구였다 — 그대로 구현하면 반경이 정사각형이 된다. `tech`가 `tile_bush.mjs`가
같은 이유로 이미 유클리드로 바꿔 둔 전례를 찾아 적용했다.

**그랬더니 이번엔 벽 윗면이 관목 울타리로 보였다.** 숲에서 **벽이 실제 수풀보다 더 수풀처럼**
보이는 상태가 됐고, 이건 C11/C12가 막으려던 오독 그 자체다.

`gd` 진단: 원인은 모양이 아니라 **모티프와 구조의 불일치**다. 타일은 32px 주기로 반드시 반복되므로
**반복되는 유기물(잎)은 격자로 읽히고, 격자를 흐리려 둥글게 만들면 덤불이 된다.** 마을(널빤지)·
동굴(벽돌)이 같은 반복인데 통과한 이유는 **인공 구조물이라 규칙성이 옳기 때문**이다.
→ 숲 벽도 인공 구조물(**이끼 낀 돌담**)로. C12 26.7 → **58.4 실측**.

그리고 이때 만든 규칙이 진짜 성과다 — **연속량 대신 0/1로 판정한다**:
- **C13 / W8 / A22**: 숲 벽에 초록 **0px**. *"숲에서 초록은 전부 통과 가능하다"*
- **P7 / A23**: 소품에 수풀색 **0px**. `작은 덤불`(숲)이 은신처와 같은 색이라 **거짓 어포던스**였다.
  가독성 문제가 아니라 설계 오류라 폐기했다

### 배운 것 7 — 코디네이터의 가설도 반증 대상이다

UI 글자 겹침에서 **코디네이터가 "웹폰트 로딩 타이밍"을 원인으로 지목**하고 그 방향으로 지시했다.
`tech`가 고치기 전에 반증했다: *"깨진 구간의 폭이 정확히 `로딩 중...` 문자열 폭과 일치하고
다른 줄은 전부 멀쩡하다."*

진짜 원인은 **`preload()`의 `"로딩 중..."` 텍스트가 `create()` 이후에도 파괴되지 않고
정확히 같은 좌표(`height/2` = `height*0.5`)에 겹쳐 있던 것**이었다. **폰트는 한 줄도 안 건드렸다.**

> 세션 4의 봇 도주 건과 같은 구조다. 이번엔 **지시한 쪽이 틀렸고 실행하는 쪽이 잡았다.**

### 다른 결함 3건 (전부 실측으로 발견)

1. **로고에 체커보드가 픽셀로 구워져 있었다** — 편집기의 투명 표시 격자가 그대로 저장됨.
   가장자리에서 무채색만 flood fill로 알파 0 처리(`scripts/fix-logo-alpha.mjs`).
   원본은 `public/assets/source/logo_title_raw.png`에 백업. **타이틀은 영상 첫 프레임이자 PDF 대표 이미지다**
2. **판정 PNG에 드롭섀도가 빠져 있었다** — `preview-tiles.mjs`가 `tile_shadow`를 안 그렸다.
   §9가 "최대의 베팅"이라 지목한 그림이 **3단 중 1단을 빼고** 찍힐 뻔했다
3. **`propPlacement`의 `ANCHORS`가 모듈 로드 시 1회 캐시였다** — `MAP_ROWS`가 라이브 바인딩이 되면서
   forest/cave에서 **영원히 village의 앵커로 소품 회피를 판정**할 뻔했다(다른 맵의 미션 마커 위에 소품)

### 스펙 모순 2건 — 둘 다 `tech`가 조용히 우회하지 않고 반려했다 (하네스 15)

1. **C1-W 적용 범위** — W2("12개 프레임 전부")와 §3.2("variant 1·3은 전면이 윗면")가 동시 만족 불가.
   → variant 0·2 6개로 범위 한정. 문턱값 불변. **`ART.md` 원본도 같이 정정**(안 하면 다음 세션에 재생산).
   **`ART.md` §6의 기계 검사 A15는 처음부터 옳게 적혀 있었다** — 사람용 문장과 기계용 문장이 갈라져 있었다
2. **C11/C12의 `wall̄` 미정의** — `tech`가 두 해석으로 측정(전체 26.7 실패 / 앞면만 49.0 통과)하고
   **자기 권한으로 색·좌표를 고쳐 통과시키지 않고 반려했다.** `gd` 판정: **프레임 전체**가 맞다.
   근거 — C11/C12는 *"이 덩어리를 지나갈 수 있는가"*를 묻고, variant 0에서 윗면이 **53%로 과반**이다

### 결정한 것

- **D10** — 조건과 적용 대상을 같은 줄에 쓴다. "전부"라고 쓰고 싶으면 그 "전부"가 몇 개인지 센다
- **D11** — 숲 벽을 이끼 낀 돌담으로. 색이 아니라 재질을 바꾼다. C13/P7 신설
- **D12** — 발주를 재해석하면 스펙을 쓰기 전에 되묻는다
- **GDD 10장 개정** — WON'T에서 "맵 2개 이상" 해제(사용자 승인). 대신 SHOULD 목록을 **비웠다**:
  결과 화면 카피바라 카드·공유 이미지 → WON'T, 아트 재작업 0회 / 밸런스 라운드 0회 / 시각 판정 1회.
  `SPEC_BOT_FLEE_FIX.md` 폐기(배너만 남김)
- **밸런스 동결(D8) 유지.** `balance.ts`의 게임 수치 변경 **0건**. 추가된 건 `TARGET`의 맵 판정 문턱뿐

### 확인 못 한 것 (하네스 14) — 다음 세션에서 반드시

- **forest·cave 맵을 브라우저에서 본 적이 없다.** village(MAPS[0])만 확인했다. 맵 선택이 랜덤이라
  나머지 둘이 안 걸렸다. **`--map=N`은 시뮬 CLI에만 있고 브라우저에는 없다**
- **세 맵을 사람이 플레이해 본 적이 없다.** 클리어 가능 여부가 미확인이다. cave 1.7%가 봇 결함인지
  맵 결함인지 구분되지 않았다. **랜덤이라 심사자가 2/3 확률로 미검증 맵을 뽑는다 — 이게 최대 위험이다**
- **`verify` 에이전트를 이번 세션에 한 번도 호출하지 않았다.** 완료 기준 대조가 안 됐다
- **`playtest` 에이전트도 호출하지 않았다.** 위 표는 `tech`가 돌린 `npm run sim` 원시 출력이지
  `playtest`의 분석이 아니다. forest `stuckRuns` 27.6%의 인과는 **손 진단이고 계측이 아니다**
- **이번 세션 변경분이 GitHub Pages에 배포되지 않았다.** 하네스 13상 8/9까지 끝내야 한다
- BGM·효과음은 이번 세션에 건드리지 않았고 여전히 **들어본 사람이 없다**

### 남겨둔 사소한 것

- 커스터마이즈 화면의 항목 라벨(몸/옷/모자)이 어두운 패널 위 회색이라 대비가 약하다
- Vite CJS 경고는 세션 2 판정대로 그대로 둔다(고치면 `npm run sim`이 깨진다)

## 2026-08-07 (세션 4) — 게임이 실제로 플레이 가능해짐

> **다음 세션은 이 항목의 「바로 시작하기」부터 읽으면 된다.**

### 🚀 바로 시작하기 (다음 세션 첫 행동)

**`docs/SPEC_ZONES.md`를 `tech`에게 넘겨 구현시킨다.** 스펙은 완성돼 있고 구현만 남았다.

우선순위는 스펙 §3~§6에 번호로 박혀 있다. **§8에 폴백(시간 없을 때 앞에서부터 버리는 순서)도 있다.**

1. **§3 벽 3단 구조** (`tile_wall` 128×96) — 이웃 마스크로 변형 선택 + `tile_shadow.png`
   → 단일 항목으로 체감 개선이 가장 크다. **이것만 해도 맵이 달라진다**
2. **§4 소품 레이어** (`props.png` 128×96) — 구역당 4종, 결정적 배치. **충돌 없음 → sim 무관**
3. **§5 구역 3종** — 세로 3행 = 동굴 / 집터 / 숲
4. **§6 은신 시각화** + `HIDING.ENABLED` 켜기

구현 후 **반드시 브라우저로 직접 볼 것.** 지금까지 "안 보고 만든 것"이 매번 문제였다.

### 한 일 (게임 기능)

- **미션 미니게임 3종** — M1 배선 잇기(드래그) / M2 온도 맞추기(SPACE) / M3 순서 기억(클릭·1~4키).
  "E 누르고 대기" 게이지를 실제 퍼즐로 교체. 어몽어스식
- **BGM + 효과음** — mp3를 받지 않고 **Web Audio로 합성**. 외부 오디오 에셋 0건.
  `M` 키 음소거. 타이틀 첫 클릭에서 시작(자동재생 정책)
- **캐릭터 리디자인** — 사용자 레퍼런스 반영. 카피바라: 낮고 넓게 + **어두운 주둥이 패치**(카피바라다움의 핵심).
  고블린: 작고 웅크리게 + 옆으로 뾰족한 귀
- **타일 개선** — 바닥 변형 4종 + 시드 스티플, 벽 줄눈 하이라이트
- **타이틀 로고** — 사용자가 Gemini로 생성. **이 프로젝트 유일의 비(非)코드 생성 에셋**
- **게임명 변경** — 「이세계 카피바라」. 저장소 주소(`capybara-escape`)는 배포 URL 때문에 유지
- **세계관 문구** — *"이세계에 떨어진 카피바라. 여긴 고블린의 서식지다."* 스토리 시스템 없이 톤만
- 글자 겹침 수정 — 한 Text + `align:'center'`가 한글 폴백 폰트에서 줄 내부 정렬을 깨뜨렸다

### 배운 것 4 — 반증 게이트가 멀쩡한 코드를 지켰다

봇 도주 속도가 이론값의 15%뿐이라 "벡터 합 상쇄"를 의심했다.
`gd`가 **고치기 전에 반증할 기회**를 설계했다: 판정 기준(≥10% 착수 / <5% 기각)을 **측정 전에** 고정.

측정 결과 **반전율 3.93% → 가설 기각**. 더 결정적으로, 원인으로 지목된 폴백 경로는
**184만 틱 중 0번 발동**했다. → **`bot.ts`를 건드리지 않았다.**

> 이 게이트가 없었으면 멀쩡한 코드를 뜯어고치고 나서야 원인이 아님을 알았을 것이다.

### 배운 것 5 — 안 보고 만들면 안 된다

`gd`는 매번 "좌표를 렌더해서 본 적 없다"고 정직하게 보고했고, `tech`는 브라우저가 없었다.
그래서 **아무도 결과를 안 본 채로** 스프라이트가 쌓였다.

이번에 코디네이터가 미리보기 PNG를 직접 열어보고 2건을 잡았다:
1. **고블린 측면이 형태로 안 읽힘** — 귀가 외곽선에 흡수돼 사라졌고, 머리·몸통 경계선이 없었고,
   허리띠가 실루엣 전 폭을 가로질렀다 (원인 3개 전부 코드로 확인)
2. **옷이 카피바라를 지움** — 몸통 전체를 덮는 단색 사각형이라 "색깔 상자에 담긴 머리"로 보였다.
   → 아랫변을 올려 "띠"로 만들자 실루엣이 살아났다

> 코디네이터가 그림을 보고 "여기가 이상하다"를 짚는 루프가 모델을 올리는 것보다 효과가 컸다.

### 지금 상태 — GDD 10장 MUST

| 항목 | 상태 |
|---|---|
| S1~S4 화면 전환 완주 | ✅ |
| 커스터마이즈 3레이어 + 인게임 반영 | ✅ |
| 탑다운 이동 + 벽 충돌 | ✅ |
| 고블린 3마리 FSM + 시야 | ✅ (시야 부채꼴 표시 = 사용자가 좋아한 "레이더") |
| 미션 3종 + 5개 완료 → 탈출 | ✅ |
| 타이머 / 체력 / 승패 | ✅ |
| 헤드리스 시뮬 + playtest 1회 이상 | ✅ (2라운드) |
| GitHub Pages 배포 | ✅ https://leehaejin02.github.io/capybara-escape/ |
| **60초 무편집 플레이 영상** | ❌ **미착수** |
| **PDF 2종** (게임 소개 / AI 활용 기술) | ❌ **미착수** |

**게임 기능은 사실상 완성.** 남은 진짜 과제는 **영상 + PDF 2종**이다. 누락 시 심사 제외.

### 확인 못 한 것 (하네스 14) — 다음 세션에서 반드시

- **미션 퍼즐 3종을 사람이 플레이해본 적이 없다.** 타입체크·빌드·Node 스모크만 통과했다
- **BGM 소리를 들어본 사람이 없다.** 노드 그래프 구조만 확인했다
- **새 캐릭터가 게임 화면에서 어떻게 보이는지 모른다.** 미리보기 PNG로만 봤다
- 고블린 측면이 아직 약하다(코디네이터 판정: "초록 사각형에 안테나"에 가깝다).
  기능상 문제는 없다고 보고 넘겼다 — 초록+앰버 눈이라 갈색 카피바라와 즉시 구분된다

### 밸런스 — 동결 상태 유지

D8에서 동결했다. 클리어율 12.9%(목표 35~55%)로 **미달인 채 제출**한다.
이유: 봇이 도주를 실행하지 못해 **12.9%는 사람 클리어율의 하한**이고, 그 상태에서 게임 수치를 깎으면
사람에게 터무니없이 쉬워진다. `TARGET`도 GDD 11장 7번도 완화하지 않았다.
**`verify`는 이 항목을 정상적으로 실패 처리해야 한다.**

> **은신을 켜면 클리어율이 오른다.** 켠 뒤 `playtest`를 다시 돌려 **측정만** 하고
> 수치는 건드리지 않는다(D8 방침).

### 남은 일정 (마감 2026-08-10)

| 날짜 | 할 일 |
|---|---|
| **8/8** | `SPEC_ZONES` 구현 → 브라우저 확인 → **영상 촬영** |
| **8/9** | **PDF 2종** + 최종 배포 확인. 하네스 13상 배포는 이날까지 |
| 8/10 | 예비 + 접수 |

**영상 촬영 시 피격·완료 미션·잔여 시간을 기록해 둘 것** — 그게 사람 클리어율의 첫 실측 데이터이고,
D8-e가 "봇 기준 추정이 사람에게 얼마나 빗나가는지"의 반증 자료로 지목한 값이다. 추가 비용 0.

## 2026-08-06~07 (세션 3) — sim 구현 + 배포 + playtest 첫 측정

### 완료된 MUST (GDD 10장)

- [x] **GitHub Pages 배포 + 공개 저장소** — https://leehaejin02.github.io/capybara-escape/
- [x] **헤드리스 시뮬 + playtest 밸런싱** — 1회차 측정 완료 (합격은 아님)

### 한 일

- `docs/RULES.md` 신설 (gd) — 50×37 맵, FSM 판정식, 봇 정책. 헤드리스로 계산 가능한 수준
- `src/sim/` 규칙 구현 (tech) — map/raycast/vision/movement/goblin/mission/bot/world. **`stub` 해제**
- `docs/ART.md` 신설 (gd) — 16색 팔레트 + 스프라이트 명세
- `docs/ASSET_CREDITS.md` 신설 — sprite-gen(Apache-2.0) 도입 기록
- sprite-gen 환경 구축 — Python 3.12.10 설치, venv + Pillow/numpy
- `playtest` 첫 호출 — 1200회 측정

### 배운 것 1 — 문서의 안전 논증에 구멍이 있었다

`RULES.md` §3.5가 "터널링 없음"을 논증하는데, 그 논증은 **틱당 이동(≤2.5px)만** 커버한다.
`HIT.KNOCKBACK_PX`(64px)는 그 밖이었고, 한 번에 이동시키면 **플레이어가 벽을 뚫고 맵 밖으로 나갔다.**
tech가 구현 중 실측으로 발견 → 서브스텝 분할로 수정. 재현 시드·틱을 코드 주석에 기록.

> 문서가 "안전하다"고 논증했다고 해서 안전한 게 아니다. 논증의 **적용 범위**를 확인해야 한다.

### 배운 것 2 — 지표가 다른 지표의 증상일 수 있다

`stuckRuns`(봇 정지 감지)가 2/100으로 0이 아니었다. 봇 버그인지 밸런스 문제인지가 쟁점이었다.

`playtest`가 **반증 실험**으로 판정했다: `MAX_HP`를 99로 올려 사망을 제거하자
`stuckRuns`가 줄기는커녕 **2·1·2 → 7·14·12로 급증**했다.
→ 원래 낮았던 건 봇이 잘 움직여서가 아니라 **60초 체크포인트 전에 이미 죽어서 검사를 못 받았기 때문**.
`stuckRuns`는 처음부터 치사율의 파생 지표였다.

> "지표가 좋아 보인다"가 "문제가 없다"를 뜻하지 않는다. 그 지표가 측정될 기회 자체를 잃었을 수 있다.

### playtest 1차 결과 — 전면 미달

6시드 × 200회 = **1200회** 합산 (시드 간 clearRate 표준편차 1.7%p)

| 지표 | 목표 | 실측 |
|---|---|---|
| 클리어율 | 35~55% | **7.0%** |
| 평균 잔여시간(승리 시) | 10~35s | **39.4s** |
| 평균 피격 | 1~2회 | **2.84** (MAX_HP 3) |
| timeout:hp0 | 6:4 | **1.4% : 98.6%** |

**단일 원인: 치사율 과다.** 시간은 오히려 남는다. 병목은 미션 소요가 아니라 고블린 접촉.

### 발견한 모순 → **해소됨** (D7-a)

**`GOBLIN.CHASE_SPEED`(95) > `PLAYER.BASE_SPEED`(90)** 인데,
GDD 6장 본문과 `balance.ts` 주석은 둘 다 **"플레이어가 더 빠르다"**고 적혀 있었다. 셋이 서로 모순.

`gd` 판정: **수치가 틀렸다.** 두 문장이 *서로 독립적으로* 같은 의도를 말하고 있었으므로,
오타 둘이 같은 방향으로 날 확률보다 숫자 하나가 잘못 굳었을 확률이 높다.
95에서는 대시를 끊김 없이 써도 3초당 9px밖에 못 번다 → 실질 도주 수단이 "시야 이탈 3초" 하나뿐이었다.

→ **95 → 75.** 부등호 `CHASE < BASE_SPEED`를 **불변식으로 승격**(GDD 6장 + balance.ts).
앞으로 이 부등호는 밸런싱 대상이 아니고, 상대 속도 조정 시 움직이는 쪽은 **항상 고블린**이다.

### 배포 — 5회 실패 후 방식 전환

`actions/deploy-pages` 경로가 이 저장소에서 프로비저닝되지 않았다(5회 모두 `deployment_queued` 정체).
빌드·워크플로·브랜치 정책·Pages 서비스 전부 정상임을 확인한 뒤 **`gh-pages` 브랜치 배포로 전환** → 한 번에 성공.
Pages 배포 ID는 커밋 SHA이고, 서버에 걸린 배포가 후속 배포를 전부 막는다는 것도 확인했다.

> 하네스 13(마감 당일 첫 배포 금지)이 값어치를 한 지점. 8/10이었으면 여기서 끝났다.

### ⚠️ 배운 것 3 — 측정 기구가 고장 났을 수 있다 (라운드 2, 미해결)

라운드 2(`CHASE_SPEED` 95→75) 결과: 클리어율 7.0% → **12.9%**. 방향은 맞지만 여전히 미달.
`gd`의 D7-d 분기표상 "20% 미만" 갈래이고, 그 갈래가 **봇 실효 이동속도 측정**을 지시했다.
그 진단이 숫자보다 중요했다.

| 측정 | 값 |
|---|---|
| 이론상 이격 속도 (`BASE 90 − CHASE 75`) | 15 px/s |
| **실측 이격 속도** (CHASE 중 고블린 300px 이내 전 틱) | **2.2 px/s** — 이론값의 15% |
| 순간 속도 실현율 (벽 충돌 손실) | 90.8% — 9% 손실로는 설명 안 됨 |
| 자유이동 경로 우회 배수 | 평균 2.38× (p90 9.9×, p99 37×) |
| 20초 넘는 이동 구간 | 전체 이동거리의 **56.6%** |

**유력 원인**(playtest 분석, tech 확인 필요): `src/sim/bot.ts`의 `computeFleeDir`가
위협들로부터의 **단위벡터 합**을 쓴다. 고블린 2마리 이상이 서로 다른 방향에서 잡히면
**합이 상쇄돼** 도주 방향이 흔들리거나 0에 가까워진다. 여기에 384px 간격의 성긴
12노드 격자가 LOS 차단 시마다 개입한다.

**왜 심각한가**: `RULES.md` §7.0 — *"게임을 못 고쳐서 봇을 고치면 측정이 무의미"*.
반대도 성립한다. **고장 난 자로 재면서 게임 수치를 깎으면 사람에게는 터무니없이 쉬운 게임이 된다.**
`gd`도 D7-e에 "봇은 격자를 우회하지만 사람은 직선으로 움직인다"고 이미 적어 뒀다.
봇이 못 도망치는 걸 보상하려고 시야를 90까지 내리면 그 오차가 그대로 게임에 박힌다.

→ **VISION 조정 후에도 목표에 못 들어가면, 다음 후보는 `balance.ts` 축이 아니라
`bot.ts`의 FLEE 벡터합 / 격자 밀도다.**

### 검증된 예측 2건 (라운드 2)

- **D7-c (gd)** — avgHits는 독립 축이 아니라 hp0 비율의 파생 지표다.
  산술 모델(`0.1292×1 + 0.0867×1.5 + 0.7842×3 = 2.61`)이 **실측 2.61과 소수점 둘째 자리까지 일치.** 확정.
- **stuckRuns (playtest)** — 치사율이 내려가면 같이 준다. hp0 91.7%→78.4% 동안 1.25%→0.83%. 맞았다.

`playtest`는 자기가 인용한 효율 추정도 정정했다: CHASE 축이 **수확 체감** 구간에 들어섰다
(95→85 구간 0.408 %p/1% → 85→75 구간 **0.136**). gd가 말한 "VISION 대비 2배 효율" 격차는 사라졌다.

**라운드 4 입력값**(측정해 둠): 승자 잔여시간 중앙값 **48.1s**, elapsed 중앙값 131.9s.
라운드 3에서 분포가 바뀌면 재계산 필요 — 지금은 참고치.

### 밸런스 동결 결정 (D8, 2026-08-07)

라운드 3을 **하지 않기로** 했다. `balance.ts` 게임 수치 변경 **0건**.

**이유 — 보정의 부호를 모른다.** 봇이 도주를 실행하지 못하므로 **12.9%는 사람 클리어율의
추정치가 아니라 하한**이다. 진짜 값이 35~55%의 위인지 아래인지도 모르는 상태에서
`VISION` −36% 보정은 조정이 아니라 도박이다. 게다가 봇이 고쳐지면 클리어율은 위로 뛰므로
지금 확정한 값은 **사전에 확정적으로** 다시 재야 한다 — 봇의 결함을 게임 수치에 영구히 새기는 셈이다.

**손계산으로 드러난 구조**(추정, 실측 아님): `CHASE 75 < BASE 90` 불변식 때문에
**계속 움직이는 사람은 원리적으로 맞지 않는다**(이격 +15px/s라 접촉 반경 24px에 도달 불가).
반대로 **미션 중(부동)엔 탐지되면 `(140−24)/75 = 1.55초`에 반드시 맞는다.**
→ 사람의 난이도는 통째로 *"8~12초짜리 안전한 창을 고를 수 있는가"* 하나이고,
`VISION`이 그 창 크기를 정하는 **유일한 값**이다. 140→90은 5개 연속 성공 확률 18%→39% —
난이도를 반으로 깎는다.

**제출 전략**: `TARGET`을 넓히지 않았고 GDD 11장 7번도 완화하지 않았다.
**미달인 채로 제출하고 `verify`는 이 항목을 정상적으로 실패 처리한다.**
「AI 활용 기술 문서」가 요구하는 건 완성된 밸런스가 아니라 파이프라인의 구조다 —
*"에이전트가 자기 측정 기구의 고장을 발견하고 그 자로 재기를 거부했다"*는 기록이
맞춘 숫자 한 줄보다 강한 물증이다.

**정정**: D7-b의 "CHASE가 VISION 대비 2배 효율"은 두 점으로 그은 직선의 외삽이었고 깨졌다
(0.408 → 0.136 %p/상대1%). D7-d의 "83으로 이분" 갈래도 함께 무효.
→ 원칙 추가: **축 효율은 측정된 구간 안에서만 인용한다.**

**공짜 검량점**: MUST인 60초 플레이 영상이 곧 **사람 1회분 실측**이다.
촬영할 때 피격·완료 미션·잔여 시간을 기록하면 위 손계산의 첫 반증 데이터가 된다. 추가 비용 0.

### 반증 게이트 결과 — **가설 기각. `bot.ts`를 건드리지 않는다** (2026-08-07)

`SPEC_BOT_FLEE_FIX.md` 0단계. 판정 기준을 **측정 전에** 고정해 두고 쟀다
(≥10% 착수 / 5~10% 회색 / <5% 기각).

| 항목 | 값 |
|---|---|
| FLEE 틱 총합 | 1,843,099 |
| **방향 반전율** (직전 FLEE 틱과 90° 초과) | **3.93%** → **기각** |
| 시드별 범위 | 3.67 ~ 4.17% (6시드 전부 5% 미만) |
| 표준오차 | ±0.014%p — 문턱과의 차이 1.07%p는 노이즈가 아니다 |

**메커니즘 단서 2건**

- **B2 폴백(`sum = -bot.facing`) 발동 0건.** `gd`가 "180° 자기유지 진동"의 원인으로 지목한
  경로가 184만 틱 중 **한 번도 실행되지 않았다.** 의심한 메커니즘이 실제로 일어나지 않는다
- 벡터 상쇄 가설의 *방향*은 맞았다 — 동시 위협 1마리 3.48% vs **2마리 이상 7.03%**.
  다만 2마리 이상 상황이 전체 FLEE 틱의 **12.5%뿐**이라 전체를 지배하지 못한다

→ **이격속도 2.2 vs 15px/s의 주원인은 방향 반전이 아니다.** 격자 라우팅 우회(SPEC의 B3 계측 대상)나
90° 미만의 부분 이탈각 등 다른 원인을 봐야 한다. **이건 추정이고 이번 측정 범위 밖이다.**

> **이 게이트가 없었으면 멀쩡한 코드를 뜯어고치고 나서야 원인이 아니었음을 알았을 것이다.**
> 코드를 고치기 전에 가설을 반증할 기회를 주는 절차가 실제로 값을 했다.
> 「AI 활용 기술 문서」에 들어갈 사례다 — 에이전트가 자기 가설을 스스로 기각했다.

### 다음 할 일

> 우선순위는 D8-b가 날짜로 고정했다. **①씬 → ②반전율 측정(비용 0) → ③8/8에 여유 있을 때만 bot 수정 → ④재측정.**
> ②는 완료됐고 **기각이라 ③④가 사라졌다.** 남은 건 씬뿐이다.

1. **씬 S1~S4 + 커스터마이즈** ← 지금 진행 중. 무조건 우선
2. **스프라이트 생성 스크립트** — `docs/ART.md` 명세대로. 가장 어려운 건 `capy_body_01` right 3/4
   (여기서 리컬러 64프레임 + flipX 8프레임 + 오버레이 9종 앵커가 파생된다)
3. **렌더링 씬** — S1~S4. sim 상태를 그리기만 한다
4. 커스터마이즈 3레이어 합성 + 인게임/결과 반영
5. 미션 3종 UI
6. 마감 전날: 60초 플레이 영상 → YouTube, PDF 2종

## 2026-08-06 (세션 2) — 스캐폴딩 + 헤드리스 시뮬 러너

**파이프라인 전 구간을 처음으로 한 바퀴 돌린 세션.** `director → gd → 승인 → tech → verify`.

### 한 일

- **GitHub 공개 저장소 생성 + push** — https://github.com/leehaejin02/capybara-escape (브랜치 `master` → `main`)
- `director` 호출 → 메타 프롬프트 + 라우팅 + 승인 항목 6건 도출
- `docs/DECISIONS.md` 신설 (D1~D6)
- `gd` → `src/config/balance.ts` (GDD 5·6·7장 수치 이식, import 0개 / 파생식 0개)
- `tech` → Vite + Phaser 3 + TS 스캐폴딩 + `npm run sim` + `scripts/check-boundary.mjs`
- `verify` → 완료 기준 11항목 + 하네스 6항목 **직접 재실행** 감사

### 이 세션의 핵심 — 경계 장치가 "3중"이 아니었다

착수 시엔 `check-boundary.mjs` + `tsconfig.sim.json` + 런타임 크래시의 **3중 방어**를 기대했다.
`tech`가 구현 중 **실측으로 2차가 작동하지 않음을 발견**했고(sim에 phaser import를 넣어도 `tsc`가 exit 0),
사용자가 이를 독립 재현했으며(`paths` 리맵 우회도 실패), `verify`가 문서에 남은 과장 표현 2곳을 잡아냈다.

→ **실질 게이트는 `scripts/check-boundary.mjs` 하나뿐이다.** 상세는 `docs/DECISIONS.md` "D1 정정".
→ 그래서 검사기를 `npm run sim`뿐 아니라 **`npm run build`에도** 걸었고, 스캔 0개면 스스로 exit 1 하게 했다.
→ `verify`가 import 형태 **13종**으로 교차 검증 (타입 전용·동적·`require`·다중행·주석/문자열 안 가짜 import).
   **오탐·미탐 0건.**

> 에이전트가 자기 설계의 오류를 실측으로 잡고, 다른 에이전트가 문서의 과장을 잡았다.
> 「AI 활용 기술 문서」에 그대로 들어갈 사례다.

### 결정한 것

`docs/DECISIONS.md` D1~D6 참조. 요약: `@types/node`만 승인 / tsc 선컴파일 / CLI 종료코드 0·1·2 /
스텁 시뮬 수용 / 씬 껍데기 안 만듦 / `balance.ts`는 `gd` 소유.

### 확인 못 한 것 (하네스 14)

- ~~`npm run dev` 화면 육안 확인 미실시~~ → **해소.** 브라우저에서 직접 확인: 타이틀 "Capybara Escape"가
  800×600 캔버스에 렌더되고, Phaser v3.90.0이 WebGL + Web Audio로 부팅. **콘솔 에러 0건.**
- **밸런스 수치는 전부 미검증 가설.** 게임 규칙이 없어 `playtest` 측정 자체가 불가능 (D5, 명시적 생략)
- `check-boundary.mjs` 알려진 한계 2건: 템플릿 리터럴 동적 import, 변수로 만든 지정자 (정적 스캐너의 구조적 한계)

### Pages 배포 — 1·2차 실패와 원인 (하네스 13이 값어치를 한 지점)

`gh api ... -f build_type=workflow`로 Pages를 켠 직후 배포를 돌렸다.

| 시도 | 결과 | 원인 |
|---|---|---|
| 1차 (push) | 빌드 ✅ / 배포 ❌ | `deployment_queued`로 10분 정체 후 타임아웃. Pages 사이트 프로비저닝이 끝나기 전에 첫 배포가 들어감 |
| 2차 (`workflow_dispatch`, 같은 커밋) | 즉시 ❌ | **Pages 배포 ID = 커밋 SHA.** 1차에서 그 SHA가 "취소됨"으로 박혀, 같은 커밋 재실행은 즉시 cancelled |

**배운 것: 같은 커밋으로 재실행하면 영원히 안 된다. 새 커밋 SHA가 필요하다.**
확인한 것 — `github-pages` 환경의 브랜치 정책은 `main` 허용으로 정상, 기본 브랜치도 `main`.
워크플로·`base` 경로·빌드 산출물에는 문제가 없었다 (빌드 job은 두 번 다 13초에 통과).

> 이걸 8/10에 처음 돌렸으면 빌드가 멀쩡한데 사이트가 안 뜨는 상태로 마감을 맞았을 것이다.
> 하네스 13("마감 당일에 처음 배포하지 않는다")이 정확히 이 상황을 위해 있다.

### 남겨둔 사소한 것 (지금 고치지 않기로 함)

- **Vite 경고**: `vite.config.ts`가 CJS로 로드된다는 경고. 해결책인 `package.json`에 `"type": "module"`
  추가는 **`npm run sim`을 깨뜨린다** — `tsconfig.sim.json`이 CommonJS로 출력하는데
  `dist-sim/*.js`가 ESM으로 해석되기 때문이다. 고치려면 sim 출력 모듈 형식까지 같이 바꿔야 하므로
  경고를 남겨둔다. 빌드·실행에는 영향 없음
- 캔버스가 페이지 좌상단에 붙어 있다 (센터링 CSS 없음). 렌더링 씬 작업 때 같이 처리한다

### 발견한 GDD 불일치 (미해결)

맵 세로 **1200px ÷ 타일 32px = 37.5** — 정수 타일이 아니다 (가로는 1600÷32=50으로 정상).
`gd`가 GDD 7장(1200 → 1184 또는 1216)이나 8장 중 하나를 고쳐야 한다. **타일맵 생성 전까지는 미룰 수 있다.**

### 다음 할 일

1. **`gd` → 맵 세로 크기 불일치 해소** (타일맵 착수 전 필수)
2. **`gd` → sim 게임 규칙 스펙 확정** — 이동·충돌·고블린 FSM·시야·미션 진행을 헤드리스로 판정 가능한 형태로
3. **`tech` → `src/sim/`에 규칙 구현 + 봇 정책** → 이때 `stub: true`를 내린다
4. **`playtest` 최초 호출** — 3번 직후. 이때까지 "밸런스 맞췄다"는 말을 쓰지 않는다 (하네스 16)
5. `GameScene` 등 렌더링 씬 — sim 상태를 그리기만 한다
6. **GitHub Pages 배포 파이프라인 — 8/9까지.** 마감 당일 첫 배포 금지 (하네스 13)
7. 마감 전날: 플레이 영상 촬영 → YouTube, PDF 2종

## 2026-08-06 (세션 1) — 프로젝트 개설

### 한 일
- NAN 2026 대회 조건 조사 (접수 마감 8/10, 본선 9/4~6 48시간, 주제 현장 공개, 제출물에 **AI 에이전트 설계서·디렉팅 명세서** 포함)
- 레퍼런스 확인: *In the Van* = 2D 탑다운 픽셀아트 생존 시뮬
- `docs/GDD.md` 초안 작성 (v0.1)
- 에이전트 5종 등록: `director` / `gd` / `tech` / `playtest` / `verify`
- `CLAUDE.md` 하네스 작성 (아키텍처 3 / 보안 2 / 스코프 3 / 정직성 3)

### 결정한 것
| 결정 | 이유 |
|---|---|
| 웹 + Phaser 3 + TypeScript + Vite | 심사위원이 링크만 열면 즉시 플레이. 타일맵·충돌·애니메이션 내장이라 4일 압축에 유리 |
| 싱글 플레이 + AI 고블린 | 서버·동기화·매칭이 빠지면서 4일 안에 완주 가능해짐. 어몽어스에서 "시간 내 미션 후 탈출" 구조만 추출 |
| 에이전트 5개로 시작 (7개 아님) | "처음부터 완벽한 체계 금지" 원칙. `artdir`·`chronicler`는 실제로 막힐 때 추가 |
| `src/sim/`을 Phaser 비의존으로 격리 | 헤드리스 시뮬이 가능해야 `playtest` 에이전트가 성립. **이 프로젝트 최대의 차별점이 이 경계 위에 있다** |
| 밸런스 수치를 `balance.ts` 한 파일로 격리 | gd와 playtest가 파일 하나만 보고 왕복할 수 있게 |
| 커스터마이즈는 성능에 영향 없음 | 4일 안에 밸런싱할 축을 늘리지 않기 위해 |

### 블로커 해소 — 사전 과제 명세 확보 (같은 세션, 크롬 연결 후)

공식 페이지의 「상세보기」 모달을 직접 열어 원문 확인 → `docs/SUBMISSION.md`에 정리.

**계획을 바꾼 사실 5가지**

| 발견 | 영향 |
|---|---|
| 제출물 **5종 중 하나라도 누락 시 심사 제외** | 게임 완성도보다 제출물 완비가 절대 우선. 스코프 판단 기준이 바뀜 |
| 웹 빌드 + **GitHub Pages**, **.exe 제출 불가** | Phaser 웹 선택이 정확히 맞았음 (운이 아니라 확인됨) |
| 전체 소스 동일 저장소 + **커밋 기록 유지** | 몰아 커밋 금지. 에이전트 단계별 커밋이 곧 「AI 활용 기술 문서」의 물증 |
| **30~60초 실제 플레이 영상** 필수, AI 합성·조작 금지 | 게임이 1분간 안 죽고 돌아야 함. 촬영·업로드 시간 확보 필요 |
| **「AI 활용 기술 문서」**에 *구조 설명 + AI 대상 주요 프롬프트 및 지시 사항* 요구 | 우리 접근이 정면으로 맞아떨어짐. CLAUDE.md·agents·playtest 로그가 그대로 본문 |

부수 확인: 팀당 **최대 3인** 확정(기사의 "2~5명"은 오보). 개인 참여면 5번 제출물 생략.
외부 에셋은 출처·라이선스 명시 필수, 무단 도용 시 **선발 취소**. 접수 마감 후 변경 불가.

→ 이에 따라 `CLAUDE.md`에 **대회 제출 규정 하네스 5조(9~13번)** 추가, GDD 0·8·10장 개정.

### 결정: 개인 참여 (2026-08-06)

제출물이 **4종**으로 확정됐다 (「팀원 롤 기술서」는 2인 이상 팀만 해당).

- PDF가 하나 줄어 마감 압박이 완화됨
- 대신 **「AI 활용 기술 문서」가 팀원 롤 기술서의 역할까지 흡수**한다.
  규정이 팀에게 요구하던 *"팀원별 담당 역할 / 실제로 맡아 구현한 영역 / 협업·분업 방식"* 을
  **에이전트 5종에 대입해** 쓴다 — 사람 대신 에이전트에게 역할을 나눈 구조라는 게 곧 주장이다
- "혼자서 4일에 이만큼"이라는 사실 자체가 파이프라인의 설득력이 된다

### 다음 할 일
1. **GitHub 공개 저장소 생성 + 첫 push** — 커밋 기록이 제출 요건이라 하루라도 일찍 시작할수록 유리
3. Vite + Phaser 3 + TS 스캐폴딩, `src/sim/` ↔ `src/scenes/` 경계 먼저 세우기
4. 헤드리스 시뮬 러너 `npm run sim` — **게임보다 먼저 만든다.** 나중에 붙이면 아키텍처가 이미 망가져 있다
5. `balance.ts` 초기값 이식 (GDD 7장)
6. GitHub Pages 배포 파이프라인 — **8/9까지 끝낸다.** 마감 당일 첫 배포는 금지(하네스 13)
7. 마감 전날: 플레이 영상 촬영 → YouTube, PDF 3종 생성

### 확인 못 한 것
- 신청서(Google Forms)의 실제 입력 항목 — 폼 제출은 본인이 해야 함
- FAQ Q04(시연·발표 형식), Q05(유료 API 부담) — 본선 사항이라 사전 과제엔 영향 없음
