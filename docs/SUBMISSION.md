# 사전 과제 제출물 명세 및 체크리스트

> 출처: nan2026.nhn.com → 참가 안내 → 사전 과제 「상세보기」 모달 (2026-08-06 직접 열람)
> **접수 마감: 2026-08-10** / 접수 마감 후에는 **제출 내용을 변경할 수 없다.**
> 신청 폼: Google Forms (사이트의 "참가신청" 버튼)

## ✅ 2026-08-08 재확인 — 원문 전체를 다시 열어 대조했다

사용자가 *"배포가 요구사항이 맞나? 내 기억엔 없었다"*고 제기해 **모달 원문을 처음부터 끝까지 다시 읽었다.**

**결론: 이 문서의 내용은 전부 정확했다. 고칠 곳 0건.** 특히 **배포는 요구사항이 맞다.**

- 제출물 표 1번 행 — 제출 형태 **「웹(브라우저 실행) 또는 모바일 앱(APK·테스트 링크) + 전체 소스」**, 비고 **「GitHub Pages (링크)」**
- 항목별 상세 1번 — *"심사자가 직접 플레이할 수 있는 형태로 제출합니다. 아래 중 한 가지 이상을 제공해 주세요"*
  → ① **웹 빌드: GitHub Pages 등으로 배포하여, 링크 클릭만으로 브라우저에서 바로 플레이** ② 모바일 앱: APK 또는 테스트 배포 링크
- **PC 실행 파일(.exe) 직접 제출은 보안상 받지 않는다** → 선택지가 웹·모바일 둘뿐이므로 **우리에겐 웹 배포가 사실상 강제**다
- 5번 팀원 롤 기술서에 *"2인 이상 팀인 경우에만 제출해주세요"* 문구 유지 → **개인 참여 4종 판단 유효**

> ⚠️ **규정은 「GitHub Pages **등으로**」라고 쓴다.** 호스팅을 특정하지 않는다.
> **요구되는 것은 "배포되어 있다"는 사실이지 GitHub Pages라는 수단이 아니다.**
> 이미 Pages에 올라가 있으므로 바꿀 이유는 없지만, 이 구분을 적어 둔다.

### 배포 ≠ 접수 — 무엇이 언제 잠기는가

| | 잠기나 | 언제 |
|---|---|---|
| **GitHub Pages 배포** | ❌ **안 잠긴다.** push할 때마다 갱신되고 URL은 불변 | — |
| **Google Forms 접수** | ✅ 잠긴다. PDF 2종은 파일 업로드라 제출 후 교체 불가 | 접수 시점 |

→ **배포는 일찍, 접수는 마지막에.** 오늘 배포해 두고 마감까지 계속 고친다.
→ ⚠️ **회색지대**: 접수 후에도 push하면 링크 뒤의 내용은 바뀐다. 유의사항의
  *"접수 마감 후에는 제출 내용을 변경할 수 없습니다"* 취지에 걸릴 수 있으므로 **폼 제출 이후에는 건드리지 않는다.**

### 같은 날 발견 — 참가약관이 개정됐다 (우리에겐 무영향)

사이트 첫 화면에 **「참가약관 변경 안내」** 모달이 떠 있었다. **공지·적용일자 2026년 8월 5일**,
개정 대상은 **제3조(참가 신청) 가호** — *팀대표자가 팀을 대표하여 약관에 동의하고 주최사로부터 상금 등을 수령할 권한*.

**개인 참여이므로 영향 없고, 제출물 요건은 한 글자도 바뀌지 않았다.**
다만 **8/6 열람 이후 사이트에 변동이 있었다**는 사실 자체가 기록할 값이 있다 —
이 문서의 출처가 "한 번 읽은 스냅샷"이라는 뜻이기 때문이다. **접수 직전에 한 번 더 대조한다.**

## ⚠️ 가장 중요한 조항

> **"아래 5개 항목을 모두 제출해야 하며, 하나라도 누락 시 심사 대상에서 제외됩니다."**

게임 완성도보다 **제출물 완비가 절대 우선**이다.
게임이 조금 덜 다듬어져도 전부 있으면 심사를 받고, 게임이 훌륭해도 하나가 비면 **0점**이다.
스코프 충돌이 생기면 언제나 제출물 완비 쪽으로 자른다.

## 우리가 낼 것 — **5종** (⚠️ 3인 팀 참여로 변경, 2026-08-09)

> **2026-08-06에 "개인 참여 확정"으로 기록했으나 2026-08-09에 3인 팀으로 바뀌었다.**
> 팀: **이해진**(팀장·개발) · **지승제**(기획·문서) · **유한결**(아트·영상).
> 본선 3인 전원 참석 가능 확인함. → **5번 「팀원 롤 기술서」가 새로 필요해졌다.**

| # | 제출물 | 형태 | 비고 | 상태 |
|---|---|---|---|---|
| 1 | 플레이 가능한 빌드 및 소스 코드 | 웹(브라우저 실행) + **전체 소스** | GitHub Pages (링크) | ✅ 배포 완료 |
| 2 | 플레이 동영상 | 30~60초 플레이 영상 | https://www.youtube.com/watch?v=qspGQt6AvOA | ✅ 업로드 완료 (54초, 공개) |
| 3 | 게임 소개 및 설명 문서 | 게임 개요·플레이 방법·실행 방법 | PDF | 🟡 원고만 |
| 4 | **AI 활용 기술 문서** | AI 도구·프롬프트·활용 내역 정리 | PDF | 🟡 원고만 |
| 5 | **팀원 롤 기술서** | 이름·담당 역할·실제 구현 영역·분업 방식 | PDF | 🟡 원고 작성됨 |

> ⚠️ **5번에서 절대 하면 안 되는 것.** 규정은 「담당 역할」과 **「실제로 맡아 구현한 영역」**을
> 따로 묻는다. **사전 과제 구현은 팀장 이해진이 단독 수행했고, 커밋 author가 전량
> `leehaejin02` 단일이라 검증 가능하다.** 다른 팀원이 사전 과제를 만든 것처럼 적으면
> 커밋 기록과 즉시 모순되고, 유의사항상 **선발 취소** 사유다.
> → `docs/SUBMISSION_TEAM_ROLES.md`는 **담당 역할(3인)과 실제 수행(1인)을 분리해서** 적는다.

---

### 1. 플레이 가능한 빌드 및 소스 코드 (GitHub)

심사자가 **직접 플레이할 수 있는 형태**로 제출한다. 아래 중 하나 이상:
- **웹 빌드**: GitHub Pages 등으로 배포해, **링크 클릭만으로 브라우저에서 바로 플레이** ← 우리가 택한 경로
- 모바일 앱: APK 파일 또는 테스트 배포 링크(TestFlight, Google Play 내부 테스트 등)

- 별도 **유료 라이선스 없이** 심사자가 실행할 수 있어야 한다
- 보안상 이유로 **PC 실행 파일(.exe) 직접 제출은 받지 않는다**
- 게임 **전체 소스 코드를 동일 저장소**에 포함하고, **커밋 기록을 유지**할 것
- 저장소는 **공개(public) 권장**. 부득이 비공개면 심사 계정 초대: `dl_gameai_reviewer@nhn.com`

> 💡 **"커밋 기록을 유지"가 우리에게 유리한 조항이다.**
> 에이전트 팀이 단계별로 일한 흔적(gd 스펙 → tech 구현 → playtest 반려 → gd 조정)이
> 커밋 히스토리에 그대로 남으면, 4번 문서의 주장을 **증명**하는 물증이 된다.
> 마지막에 한 번에 몰아 커밋하면 이 증거가 사라진다. → 작업 단위마다 커밋한다.

#### 배포 절차 (GitHub Pages)

워크플로 파일 `.github/workflows/deploy.yml`은 이미 만들어져 있고, `main`에 push되면 자동으로
`npm run build`(경계 검사 포함) → Pages 배포까지 돈다. **단, 저장소 Settings에서 한 번은
사람이 직접 웹 UI로 설정을 켜야 워크플로가 실제로 배포 권한을 갖는다.** 이건 `gh` CLI로
자동화하지 않았다(에이전트가 배포를 실제로 트리거하지 않도록 하기 위함) — 아래를 사용자가 직접 한다.

1. GitHub 저장소(`leehaejin02/capybara-escape`) 웹 페이지 → **Settings → Pages**로 이동
2. **Build and deployment → Source**를 `Deploy from a branch`에서 **`GitHub Actions`로 변경**
   (이걸 안 하면 워크플로가 `actions/deploy-pages`에서 권한 오류로 실패한다)
3. 이 상태에서 `.github/workflows/deploy.yml`을 포함한 커밋을 `main`에 push
   (또는 이미 push된 뒤라면 **Actions 탭 → Deploy to GitHub Pages → Run workflow**로 수동 실행 —
   워크플로에 `workflow_dispatch`가 걸려 있어 커밋 없이도 재배포 가능)
4. Actions 탭에서 워크플로 실행이 초록색으로 끝나는지 확인
5. 배포 완료 후 URL 확인: **Settings → Pages** 상단에 뜨는 링크, 예상값은
   `https://leehaejin02.github.io/capybara-escape/`
6. 실제로 브라우저에서 열어 흰 화면/404 없이 게임이 뜨는지 확인
   (특히 `vite.config.ts`의 `base: '/capybara-escape/'`가 저장소 이름과 정확히 일치해야 한다 —
   저장소 이름을 바꾸면 이 값도 같이 바꿔야 한다)

> 하네스 13("마감 당일에 처음 배포하지 않는다")에 따라 이 절차는 **마감 하루 전이 아니라 지금 미리
> 한 번 실행해 성공을 확인해 둔다.**

### 2. 플레이 동영상 (YouTube)

- **30~60초** 분량, **실제 게임 플레이 장면 중심**
- **AI를 이용한 조작·합성이나 타인 영상 도용 불가.** 실제 플레이 화면 그대로 담아야 한다
- 공개 또는 일부 공개(링크 공유) 상태로 업로드

> 이 조항 때문에 **게임이 실제로 60초 동안 끊김 없이 돌아가야 한다.**
> 스크린샷이나 목업으로 대체 불가. 촬영·편집·업로드 시간을 일정에 반드시 확보한다.

### 3. 게임 소개 및 설명 문서 (PDF)

- 게임 제목 및 **한 줄 소개**
- **게임 방법**: 목표 · 조작 · **종료 조건** 포함
- **실행 방법**: 게임 설치 및 실행 방법
- 플레이 링크 또는 설치 방법 (웹 URL / APK·테스트 링크)
- 플레이 영상 링크

### 4. AI 활용 기술 문서 (PDF) ← **우리의 승부처**

- AI 활용에 대한 **기술적 설명** (예: **구조 설명, AI 대상 주요 프롬프트 및 지시 사항**)
- **외부 에셋 / 오픈소스 출처**

> 대회 슬로건이 *"우리가 찾는 사람은 AI의 다음 단계를 설계할 디렉터입니다"* 이고,
> 이 문서가 그걸 직접 묻는 항목이다. 대부분의 참가자는 "클로드로 코딩했습니다" 수준으로 쓴다.
>
> **우리가 낼 것:**
> - `CLAUDE.md` 하네스 11조 — *AI에게 무엇을 금지시켰는가*
> - `.claude/agents/*.md` 5종 — *역할을 어떻게 쪼갰고, 왜 도구를 제한했는가*
> - `playtest` ↔ `gd` 밸런싱 왕복 로그 — *AI가 수치를 스스로 조정한 실제 기록*
> - 커밋 히스토리 — *위 주장의 물증*
> - 에이전트별 호출 횟수 집계 — *설계가 실제로 쓰였다는 증거*
>
> 이 문서는 **마지막에 쓰는 게 아니라 개발하면서 쌓이는 것**이어야 한다.
> 그래서 WORKLOG와 DECISIONS를 처음부터 쓴다.

### 5. 팀원 롤 기술서 (PDF) — **제출 대상** (3인 팀)

규정이 요구하는 항목: **팀원별 이름/담당 역할**, **실제로 맡아 구현한 영역**, **협업·분업 방식**.

원고: `docs/SUBMISSION_TEAM_ROLES.md` → `npm run build:submission` → Ctrl+P로 PDF.

- 1장 **담당 역할**: 이해진(개발·AI 파이프라인) / 지승제(기획·문서) / 유한결(아트·영상)
- 2장 **실제로 맡아 구현한 영역**: 사전 과제는 **이해진 단독 수행**임을 명시하고,
  나머지 2인의 역할은 **본선 담당**으로 적는다. 커밋 author와 어긋나지 않게 하기 위함이다.
- 3장 **협업·분업**: 사전 과제는 사람 1인 + AI 에이전트 5종의 역할 분리,
  본선은 3인 분업. 4번 문서와 서사가 충돌하지 않는다.

---

## 유의사항 (원문 요지)

- 타인의 저작물·아이디어를 무단 도용한 경우, **발생하는 모든 책임은 제출자 본인**에게 있으며 **선발이 취소**될 수 있다
- 외부 에셋(이미지·사운드 등) 사용 시 **출처와 라이선스를 4번 문서에 반드시 명시**
- **AI 도구 사용은 허용·권장**하나, 사용 도구와 활용 내역을 반드시 기재
- 제출 링크(GitHub·YouTube)는 **심사 종료 시점까지 접근 가능**하게 유지
- 접수 마감 후에는 **제출 내용을 변경할 수 없다**
- 개인정보 수집·이용 및 저작권 관련 동의가 제출에 포함된다

## 참가 조건

- 만 19세 이상 누구나 / 총 **10팀** 선발 / **팀당 최대 3인**
- 본선 2026.09.04~09.06, **48시간 전일 참여 필수**, 온라인 참여 불가 (판교 NHN 사옥)

## 미확인 사항

- [ ] 신청서(Google Forms)의 실제 입력 항목 — 폼을 직접 열어봐야 확인 가능. **제출은 본인이 해야 한다**
- [x] ~~개인 참여인지 팀 참여인지~~ → **개인 참여 확정 (2026-08-06)**
- [ ] FAQ Q04(시연·발표 형식), Q05(유료 API 사용료 부담) — 본선 관련이라 사전 과제엔 영향 없음
